#include <stdio.h>
#include <stdlib.h>
#include "hvector.h"
int main()
{
int tam = vecSizeRequest();
if(tam == -1)
    {
    tam = 5;
    }
int vector[tam];
vecIntInit(vector[],-1);
vecIntFill(vector[],tam,0);

return 0;
}
